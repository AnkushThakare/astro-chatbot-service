services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: astro_chatbot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  api:
    build: .
    env_file:
      - .env
    command: uvicorn src.main:app --host 0.0.0.0 --port 8010
    environment:
      APP_ENV: development
      APP_HOST: 0.0.0.0
      APP_PORT: 8010
      CORE_SERVICE_BASE_URL: ${CORE_SERVICE_BASE_URL:-http://host.docker.internal:8000/api/v1}
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/astro_chatbot
      ASYNC_DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/astro_chatbot
      ASTROLOGY_ENGINE_MODE: stub
    ports:
      - "8010:8010"
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data:
